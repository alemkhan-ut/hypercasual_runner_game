using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImpulseTrigger : MonoBehaviour
{
    [SerializeField] private float impulsePower_;
    private void OnTriggerEnter(Collider other)
    {
    }
}